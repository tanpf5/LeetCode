
public class Solution {
	public static int atoi(char c) {
		switch (c) {
		case 'I' : return 1;
		case 'V' : return 5;
		case 'X' : return 10;
		case 'L' : return 50;
		case 'C' : return 100;
		case 'D' : return 500;
		case 'M' : return 1000;
		}
		return 0;
	}
	
    public int romanToInt(String s) {
    	int prev = 0, ans = 0, inter = 0;
        for (int i = 0; i < s.length(); i++) {
        	int now = s.charAt(i);
        	if (now == prev) {
        		inter += prev;
        		continue;
        	}
        	if (now > prev) {
        		ans -= inter;
        		prev = now;
        		inter = now;
        		continue;
        	}
        	if (now < prev) {
        		ans += inter;
        		prev = now;
        		inter = now;
        		continue;
        	}
        }
        return ans;
    }
    
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
